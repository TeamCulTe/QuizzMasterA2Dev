package com.imie.a2dev.teamculte.quizzmaster.utils;

/**
 * Abstract class used to access the different log tags.
 */
public abstract class TagUtils {
    /**
     * Defines the default SQLite tag for logs.
     */
    public static final String SQLITE_TAG = "[SQLite]";
    
    /**
     * Defines the default error tag for logs.
     */
    public static final String COMMON_ERROR_TAG = "[commonError]";
}
